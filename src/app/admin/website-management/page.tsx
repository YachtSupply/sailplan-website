import { WebsiteManagementDashboard } from '@/components/admin/WebsiteManagementDashboard';

export const dynamic = 'force-dynamic';

export default function WebsiteManagementPage() {
  return <WebsiteManagementDashboard />;
}
